<!DOCTYPE html>
<html>
<head>
	<link href='http://fonts.googleapis.com/css?family=Lato:400,700,700italic|Pathway+Gothic+One' rel='stylesheet' type='text/css'>
  <title>Smartpaths</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body>
	<%= render :partial => 'partials/header'%>
 	<div class="container">
  	<div class="row">
  		<div class="span12">
  			<% flash.each do |name, msg| %>
					<div class="alert alert-<%= name == :notice ? "success" : "error" %>">
						<a class="close" data-dismiss="alert">x</a>
						<%= msg %>
					</div>
				<% end %>
 				<%= yield %>
 			</div>
 		</div>
 	</div>
	<%= render :partial => 'partials/footer'%>
</body>
</html>
