<%= form_for(@user_tag) do |f| %>
  <% if @user_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_tag.errors.count, "error") %> prohibited this user_tag from being saved:</h2>

      <ul>
      <% @user_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.text_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
